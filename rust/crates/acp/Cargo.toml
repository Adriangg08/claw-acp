[package]
name = "acp"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

# Agent Client Protocol (ACP) server scaffold.
# Tracks the spec at https://github.com/zed-industries/agent-client-protocol.
# See crates/acp/README.md for scope and milestones; real implementation is
# intentionally deferred — this crate only defines the module layout today.

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json.workspace = true

[lints]
workspace = true
